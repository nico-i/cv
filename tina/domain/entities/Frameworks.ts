import { generateSkillBasedCollection } from '../../infrastructure/generators/SkillBasedCollectionGenerator';

export const Frameworks = generateSkillBasedCollection([
  `frameworks`,
  `Frameworks`,
]);
